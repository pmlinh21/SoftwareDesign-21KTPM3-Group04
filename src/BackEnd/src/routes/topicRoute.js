const express = require('express');
const topicRoute = express.Router();
// const {getReportType} = require('../controllers/reportController')

// topicRoute.get("/getReportType", getReportType)

module.exports = topicRoute;